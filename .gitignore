# ---------------------------------------------------------------- .NET 构建产物
# obj/bin 每台机器都不一样，提交进去只会制造 diff 噪音（这个仓库曾经 78% 的文件是构建产物）
bin/
obj/
*.user
*.suo
.vs/

# ---------------------------------------------------------------- 前端
node_modules/
dist/
dist-ssr/
*.local

# ---------------------------------------------------------------- 日志与运行时产物
*.log
logs/
TestResults/
traces/

# ---------------------------------------------------------------- 本地配置：含真实密钥，绝不能提交
# 要提交的是模板：appsettings.json 和 build/.env.example
build/.env
appsettings.Development.json

# ---------------------------------------------------------------- 刻意不忽略
# .swagger-cache.json 必须跟着源码走：生产环境关掉了 swagger，
# Dockerfile 要把它 COPY 进镜像，否则 tools.json 里的 from 引用解析不了，
# 服务会直接启动失败。
